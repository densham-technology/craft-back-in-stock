{% import "_includes/forms" as forms %}

{{ forms.selectField({
    first: true,
    label: "Product back in stock email"|t,
    instructions: 'Choose the email you want to use when sending notifications that a product is back in stock.'|t,
    id: 'backInStockEmailId',
    name: 'backInStockEmailId',
    value: settings.backInStockEmailId,
    errors: settings.getErrors('backInStockEmailId'),
    options: commerceEmails,
    required: true,
}) }}
